[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lfmf-monitor"
version = "0.1.0"
description = "Low-frequency monitoring system using RTL-SDR"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
